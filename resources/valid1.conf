# Valid configuration file

database {
	host = "localhost"
	port = 5432
	username = "admin"
	password = "password"
}

api {
	endpoint = "/api/v1"
	timeout = 5000
}

logging {
	level = "info"
	enabled = true
}
